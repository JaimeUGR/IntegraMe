package com.integrame.app.tasks.ui.screens

import androidx.compose.runtime.Composable
import androidx.compose.ui.Modifier

@Composable
fun TaskScreen(
    taskId: Int,
    onNavigateBack: () -> Unit,
    modifier: Modifier = Modifier,

) {
    // Cargar la tarea

    // Cuando esté cargada, se muestra el scaffold superior
    // y se mostrará la pantalla específica correspondiente
}
